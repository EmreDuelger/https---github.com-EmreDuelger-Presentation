name: Deploy GitHub Pages

on:
  push:
    branches: [master, main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy auf GitHub Pages
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Summary
        run: |
          echo "## 🚀 Deployment erfolgreich!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Seiten live unter:" >> $GITHUB_STEP_SUMMARY
          echo "- **Startseite**: ${{ steps.deployment.outputs.page_url }}" >> $GITHUB_STEP_SUMMARY
          echo "- **DOM-Präsentation**: ${{ steps.deployment.outputs.page_url }}EmresPräsentationMitReveal.js/index.html" >> $GITHUB_STEP_SUMMARY
          echo "- **Agenten-Präsentation**: ${{ steps.deployment.outputs.page_url }}EmresPräsentationMitReveal.js/agenten.html" >> $GITHUB_STEP_SUMMARY
